<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campusrunner.backend.user.dao.UserDao">

    <select id="existsByUsername" resultType="boolean">
        SELECT CASE WHEN COUNT(1) &gt; 0 THEN TRUE ELSE FALSE END
        FROM users
        WHERE username = #{username}
    </select>

    <select id="findByUsername" resultType="com.campusrunner.backend.user.entity.User">
        SELECT id,
               username,
               password_hash,
               nickname,
               phone,
               avatar_url,
               common_address,
               bio,
               allow_friend_request,
               allow_search,
               message_dnd,
               role,
               status,
               created_at,
               updated_at
        FROM users
        WHERE username = #{username}
        LIMIT 1
    </select>

    <select id="countByStatus" resultType="long">
        SELECT COUNT(1)
        FROM users
        WHERE status = #{status}
    </select>
</mapper>
